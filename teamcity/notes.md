I ran TC agent inside a docker container

-You can find TC conf at: /data/teamcity_agent/conf/buildAgent.properties (there you can check if the serverURL was assigned as expected)
-Logs: /opt/buildagent/logs
